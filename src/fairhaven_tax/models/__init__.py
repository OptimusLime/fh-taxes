"""Phase-2 statistical models (hedonic, Berry shift, ratio study, CDF gap test).

Pure-function modules; side effects (parquet/JSON writes, exit codes) live in
`scripts/run_*.py`. Mirrors the shape of `fairhaven_tax.validate`.
"""
