---
phase: 01.5-oprs-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - datasets/collect_oprs.py
  - tests/test_oprs_collect_pdf.py
autonomous: true
requirements: [DATA-07, DATA-08]
must_haves:
  truths:
    - "collect_oprs.py supports prc.pdf, ch75.pdf, taxlist_<year>.pdf components"
    - "PDF endpoints implement two-request session-bound flow (cgi -> tmp PDF)"
    - "prc.pdf body is sliced from %PDF marker (HTTP-header strip workaround)"
    - "PDF cache files are valid PDFs (start with b'%PDF') and pdfplumber-extractable"
    - "--mode comprehensive now also fetches the three PDF components per parcel"
  artifacts:
    - path: "datasets/collect_oprs.py"
      provides: "Extended collector with PRC/CH75/TAXLIST PDF endpoints"
      contains: "PRC_BASE, CH75_BASE, TAXLIST_BASE, COMPONENT_PRC, COMPONENT_CH75, taxlist_component_name"
    - path: "tests/test_oprs_collect_pdf.py"
      provides: "Unit tests for PDF URL builders, header-strip, and validators"
  key_links:
    - from: "fetch_component (PDF branch)"
      to: "_validate (PDF branch)"
      via: "two-request flow + %PDF slice + pdfplumber check"
      pattern: "body.find\\(b\"%PDF\"\\)"
    - from: "parcel_needed_components"
      to: "comprehensive PDF list"
      via: "include prc.pdf/ch75.pdf/taxlist_<year>.pdf when m4 cached"
      pattern: "COMPONENT_PRC|COMPONENT_CH75|taxlist_"
---

<objective>
Extend `datasets/collect_oprs.py` with three new PDF components: prc.pdf (PRC PDF with HTTP-header strip), ch75.pdf (Chapter 75 annual notice), and taxlist_<year>.pdf (current-year tax-list). Adds D-27 (two-request session-bound flow), D-28 (HTTP-header strip), D-30 (PDF validation: %PDF marker + pdfplumber page-1 extraction).

Purpose: Unblock comprehensive collection (PLAN 02). Without these endpoints, prc/ch75/taxlist PDFs cannot be cached and the bedroom/bathroom/condition fields the Phase 2 hedonic needs remain unavailable.

Output: Extended collector module + unit tests covering the new code paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-oprs-collection/01.5-CONTEXT.md
@.planning/phases/01.5-oprs-collection/01.5-PATTERNS.md
@./CLAUDE.md
@datasets/collect_oprs.py

<interfaces>
Existing module-level constants in datasets/collect_oprs.py:60-74:
```python
M4_BASE = "https://tax1.co.monmouth.nj.us/cgi-bin/m4.cgi"
SR_BASE = "https://tax1.co.monmouth.nj.us/cgi-bin/sr.cgi"
DISTRICT = "1314"
COMPONENT_M4 = "m4.html"
```

Existing sr_component_name helper at line 182:
```python
def sr_component_name(ssi: str) -> str:
    return f"sr_{ssi}.html"
```

Existing _build_url at lines 274-280: branches on COMPONENT_M4 / sr_<ssi>.

Existing _validate at lines 252-271: HTML-only (block/lot regex match against flattened text).

Existing fetch_component at lines 283-324: single-request flow with retry loop.

Existing parcel_needed_components at lines 356-387: enumerates needed components per parcel for given mode.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PDF component constants, URL builders, and two-request fetch helper</name>
  <files>datasets/collect_oprs.py</files>
  <read_first>
    - datasets/collect_oprs.py (entire file — modifying constants block, _build_url, fetch_component)
    - .planning/phases/01.5-oprs-collection/01.5-CONTEXT.md (D-27, D-28, D-29, D-30)
    - .planning/phases/01.5-oprs-collection/01.5-PATTERNS.md (lines 26-101 for collector extension patterns)
  </read_first>
  <behavior>
    - Constants: PRC_BASE = "https://tax1.co.monmouth.nj.us/cgi-bin/prc.cgi", CH75_BASE = "https://tax1.co.monmouth.nj.us/cgi-bin/ch75.cgi", TAXLIST_BASE = "https://tax1.co.monmouth.nj.us/cgi-bin/taxlist.cgi"
    - Component names: COMPONENT_PRC = "prc.pdf", COMPONENT_CH75 = "ch75.pdf"; helper `taxlist_component_name(year: int) -> str` returns f"taxlist_{year}.pdf"
    - `_build_url` MUST accept the three new components and return the cgi URL (the FIRST request URL). Must include district, l02 (block/lot/qual), and any year param needed for taxlist.
    - New regex `_PDF_HREF_RE = re.compile(r'href=["\\\']([^"\\\']*tmp/[^"\\\']+\\.pdf)["\\\']', re.I)` extracts the temp PDF URL from cgi HTML response.
    - New helper `_fetch_pdf_two_step(session, cgi_url, component) -> tuple[int, bytes | None, int]`: GET cgi_url; if 200, regex out the tmp PDF href; resolve URL relative to cgi host; second GET on the same session; return (status_code, body, requests_made). Both requests count toward the budget — return requests_made = 2 on success, 1 if cgi step fails.
    - For COMPONENT_PRC ONLY: after second GET, slice body from b"%PDF" marker (D-28). For ch75.pdf and taxlist_*.pdf: do NOT strip (they are clean PDFs).
    - Extend `_validate` with PDF branch BEFORE HTML branch: if component endswith ".pdf": check len(body) >= 500 and body.startswith(b"%PDF"); return (False, "no_pdf_marker") on failure, (True, "ok") on success. (pdfplumber check happens in fetch_component AFTER write to keep _validate cheap.)
    - Extend `fetch_component`: when component.endswith(".pdf"), use `_fetch_pdf_two_step` instead of single `_fetch`; increment `state.requests_done` correctly via return tuple (caller responsibility); after atomic write, perform a pdfplumber-page-1 extraction sanity check; if pdfplumber fails, unlink and retry (count as failure).
    - Update the run_batch loop's request counter to honor the requests_made return from fetch_component for PDF components (refactor fetch_component to return `(ok, status, requests_made)` — update the two existing call sites for HTML components to return `requests_made=1`).
  </behavior>
  <action>
    1. Add constants PRC_BASE, CH75_BASE, TAXLIST_BASE, COMPONENT_PRC, COMPONENT_CH75 alongside existing constants block (after line 73).
    2. Add `taxlist_component_name(year)` helper next to `sr_component_name` (after line 183).
    3. Add `_PDF_HREF_RE` regex constant near `_SSI_RE` (around line 190).
    4. Extend `_build_url` (line 274) with three branches:
       - `COMPONENT_PRC`: return `f"{PRC_BASE}?district={DISTRICT}&l02={l02}"` (with hist=1 if observed in samples — confirm against any sample fetch)
       - `COMPONENT_CH75`: return `f"{CH75_BASE}?district={DISTRICT}&l02={l02}"`
       - `taxlist_<year>.pdf`: return `f"{TAXLIST_BASE}?district={DISTRICT}&l02={l02}&year={year}"` — extract year from component string via regex `r"taxlist_(\d{4})\.pdf"`.
    5. Add `_fetch_pdf_two_step(session, cgi_url, component) -> tuple[int, bytes | None, int]` per behavior block. Use urljoin or simple concatenation against `https://tax1.co.monmouth.nj.us` for the temp PDF href if it's relative.
    6. For COMPONENT_PRC, apply `body = body[body.find(b"%PDF"):]` if `body.find(b"%PDF") > 0` after the second GET. Use D-28 verbatim — do NOT apply this strip to ch75/taxlist.
    7. Extend `_validate` with PDF branch at top (after the `len(body) < 500` check).
    8. Refactor `fetch_component` to return `(ok: bool, status: str, requests_made: int)`. Update return statements: cached → `(True, "cached", 0)`; HTML success → `(True, "ok", 1)`; PDF success → `(True, "ok", 2)`; failures → `(False, status, requests_made_so_far)`.
    9. Add post-write pdfplumber sanity check inside fetch_component for PDF components: open with pdfplumber, check pdf.pages[0].extract_text() is non-empty; if it fails, unlink the cache file and retry. Wrap in try/except to handle ImportError gracefully (skip check with warning if pdfplumber unavailable — comprehensive collection assumes it's installed; add `import pdfplumber` at top).
    10. Update run_batch (line 437+) to add `state.requests_done += requests_made` instead of `+= 1`. Find the existing call site at line 449.
    11. Extend `parcel_needed_components` (line 356): when mode == "comprehensive" and m4 is cached, append `(COMPONENT_PRC, None)`, `(COMPONENT_CH75, None)`, `(taxlist_component_name(CURRENT_YEAR), None)` to needed list (only if not already cached). Add `CURRENT_YEAR = 2026` module constant; document that the operator updates this annually.
    12. Update `report_status` (line 518) to add per-PDF-component cached/pending counters mirroring m4 logic (count files matching prc.pdf, ch75.pdf, taxlist_*.pdf in each parcel dir).
    13. Add `pdfplumber` to project deps if not already present (`uv add pdfplumber` if missing — check pyproject.toml first).
  </action>
  <verify>
    <automated>cd /Users/paul/coding/fairhaven/tax_assessment && uv run python -c "from datasets.collect_oprs import COMPONENT_PRC, COMPONENT_CH75, taxlist_component_name, _build_url, _validate, _PDF_HREF_RE; assert COMPONENT_PRC == 'prc.pdf'; assert COMPONENT_CH75 == 'ch75.pdf'; assert taxlist_component_name(2026) == 'taxlist_2026.pdf'; url = _build_url('prc.pdf', '77', '80', '', None); assert 'prc.cgi' in url and 'l02=' in url; ok, status = _validate('prc.pdf', b'%PDF-1.4\\n' + b'x'*600, '77', '80'); assert ok; ok, _ = _validate('prc.pdf', b'<html>not pdf</html>' + b'x'*600, '77', '80'); assert not ok; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "PRC_BASE|CH75_BASE|TAXLIST_BASE" datasets/collect_oprs.py` returns 3+ matches
    - `grep "taxlist_component_name" datasets/collect_oprs.py` returns the helper definition
    - `grep "_PDF_HREF_RE" datasets/collect_oprs.py` returns the regex constant
    - `grep "_fetch_pdf_two_step" datasets/collect_oprs.py` returns the helper definition
    - `grep "body\[body.find(b\"%PDF\"):\]" datasets/collect_oprs.py` finds the prc-only header strip
    - `grep "import pdfplumber" datasets/collect_oprs.py` confirms import added
    - `grep "requests_made" datasets/collect_oprs.py` shows fetch_component returns 3-tuple
    - `_validate("prc.pdf", b"<html>...</html>" + b"x"*600, "77", "80")[0] is False` (no %PDF marker → reject)
    - `_validate("prc.pdf", b"%PDF-1.4\n" + b"x"*600, "77", "80")[0] is True`
    - `_build_url("prc.pdf", "77", "80", "", None)` contains "prc.cgi" and "l02="
    - `_build_url("ch75.pdf", "77", "80", "", None)` contains "ch75.cgi"
    - `_build_url("taxlist_2026.pdf", "77", "80", "", None)` contains "taxlist.cgi" and "year=2026"
    - Verify command above prints "OK" with no AssertionError
  </acceptance_criteria>
  <done>
    Module imports cleanly; all three PDF components round-trip through `_build_url` and `_validate`; `fetch_component` returns 3-tuple; pdfplumber import succeeds; verify command exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for PDF URL builders, header-strip, validators, and two-request flow</name>
  <files>tests/test_oprs_collect_pdf.py</files>
  <read_first>
    - tests/test_sr1a_parse.py (analog test layout)
    - datasets/collect_oprs.py (after Task 1 modifications — exact function signatures)
    - .planning/phases/01.5-oprs-collection/01.5-PATTERNS.md (lines 405-440 for test patterns)
  </read_first>
  <behavior>
    - test_build_url_prc: `_build_url("prc.pdf", "77", "80", "", None)` returns URL with prc.cgi base, district=1314, valid 28-char l02 token.
    - test_build_url_ch75: similar for ch75.cgi.
    - test_build_url_taxlist_extracts_year: `_build_url("taxlist_2026.pdf", "3", "33", "", None)` contains "year=2026".
    - test_build_url_taxlist_unknown_year_raises: `_build_url("taxlist_abcd.pdf", ...)` raises ValueError.
    - test_validate_pdf_rejects_html: body without b"%PDF" returns (False, "no_pdf_marker").
    - test_validate_pdf_accepts_marker: body starting with b"%PDF-1.4" + padding returns (True, "ok").
    - test_validate_pdf_rejects_too_small: 100-byte body returns (False, "too_small:100").
    - test_pdf_href_re_extracts: regex matches `<a href="/tmp/xyz123.pdf">` returning "/tmp/xyz123.pdf".
    - test_fetch_pdf_two_step_strips_prc_header: monkeypatch session.get with mock returning (1) cgi HTML containing tmp PDF href, (2) bytes prefixed with HTTP envelope before %PDF marker; verify final body starts with b"%PDF" and requests_made == 2.
    - test_fetch_pdf_two_step_no_strip_for_ch75: same flow but second response is clean PDF; verify body unchanged.
    - test_fetch_component_returns_tuple_3: cached path returns (True, "cached", 0).
  </behavior>
  <action>
    Create tests/test_oprs_collect_pdf.py modeled on tests/test_sr1a_parse.py structure:
    1. Header docstring + future imports + pytest + Path imports.
    2. Import the symbols added in Task 1: `_build_url`, `_validate`, `_PDF_HREF_RE`, `_fetch_pdf_two_step`, `fetch_component`, `taxlist_component_name`, `COMPONENT_PRC`, `COMPONENT_CH75`.
    3. Implement the 10 test functions per the behavior block. Use `monkeypatch` to inject a fake session whose `.get(url, timeout=...)` returns a stub object with `.status_code` and `.content`. Two-call sequencing via a list-pop pattern.
    4. For test_fetch_pdf_two_step_strips_prc_header, construct prc body as `b"HTTP/1.1 200 OK\r\nContent-Type: text/html\r\n\r\n%PDF-1.4\n" + b"x"*600` and assert returned body starts with b"%PDF" (the HTTP envelope was stripped).
    5. Use `tmp_path` for any file-write tests.
  </action>
  <verify>
    <automated>cd /Users/paul/coding/fairhaven/tax_assessment && uv run pytest tests/test_oprs_collect_pdf.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_oprs_collect_pdf.py exists
    - `pytest tests/test_oprs_collect_pdf.py -v` reports 10 passed, 0 failed
    - `grep "def test_" tests/test_oprs_collect_pdf.py | wc -l` >= 10
    - Tests for all of: build_url (prc/ch75/taxlist + year extraction + bad year), validate (reject html / accept marker / reject small), regex extraction, two-step flow with prc strip, two-step without strip for ch75, fetch_component 3-tuple
  </acceptance_criteria>
  <done>
    All 10 unit tests pass; pytest exits 0.
  </done>
</task>

</tasks>

<verification>
- All Task 1 acceptance grep checks pass.
- `uv run pytest tests/test_oprs_collect_pdf.py -v` exits 0 with 10+ tests.
- Manual smoke: `uv run python datasets/collect_oprs.py --help` shows the same CLI (no new flags needed; mode=comprehensive now picks up PDFs automatically).
</verification>

<success_criteria>
- collect_oprs.py extended with prc.pdf, ch75.pdf, taxlist_<year>.pdf endpoints (D-27, D-28, D-30).
- HTTP-header strip applied to prc.pdf only.
- PDF validation rejects responses lacking b"%PDF" marker.
- Two-request flow accounted for in batch budget.
- 10 unit tests pass.
- No live network access required for verification (all mocked).
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-oprs-collection/01.5-01-SUMMARY.md` documenting:
- Constants/helpers added
- Two-request flow design (sequence diagram in prose)
- pdfplumber dependency added (if applicable)
- Test count and coverage notes
- Any deviations from D-27/D-28/D-30 (with rationale)
</output>
